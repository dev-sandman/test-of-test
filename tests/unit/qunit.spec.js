QUnit.test( "tools and localstorage add", function( assert ) {
  assert.ok( true == true, "Passed!" );
});

QUnit.test( "tools changed", function( assert ) {
  assert.ok( true == true, "Passed!" );
});

QUnit.test( "localstorage changed", function( assert ) {
  assert.ok( true == true, "Passed!" );
});
